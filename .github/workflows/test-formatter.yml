name: Test Java Formatter Action
on: [workflow_dispatch]

jobs:
  format-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Google Java Formatter
        uses: ./
        with:
          gjf-version: '1.28.0'
          aosp: 'true'
          paths: 'src'
#      - name: Debug Formatter
#        uses: ./
#        with:
#          arg1: ""
#          arg2: ""
#          arg3: ""